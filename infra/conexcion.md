# Integración de Servicios

- El frontend envía datos mediante fetch() hacia el backend Flask.
- El backend recibe los datos en las rutas definidas.
- No se usa base de datos en esta etapa.
- URL temporal del backend: http://localhost:5000
